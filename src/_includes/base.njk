<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} - Tom Ip</title>
    <style>
        :root {
            --primary-color: #89CFF0;
            --text-color: #333;
            --background-color: #fff;
            --secondary-color: #6BA4C7;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }

        body {
            background-color: var(--background-color);
            color: var(--text-color);
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .header {
            padding: 1.5rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
            border-bottom: 1px solid #eee;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 1rem;
            text-decoration: none;
            color: var(--text-color);
            font-weight: 600;
        }

        .brand img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
            display: block;
        }

        .nav {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav a {
            text-decoration: none;
            color: var(--text-color);
            font-weight: 500;
            font-size: 0.9rem;
            transition: color 0.2s;
        }

        .nav a:hover {
            color: var(--primary-color);
        }

        .youtube-badge {
            background-color: #FF0000;
            color: white;
            padding: 0.25rem 0.75rem;
            border-radius: 4px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.8rem;
            transition: opacity 0.2s;
        }

        .youtube-badge:hover {
            opacity: 0.9;
            color: white !important;
        }

        main {
            flex: 1;
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .content {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
        }

        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
                padding: 1rem;
            }

            .nav {
                gap: 1rem;
                flex-wrap: wrap;
                justify-content: center;
            }

            main {
                padding: 1rem;
            }
        }
    </style>
    {% block head %}{% endblock %}
</head>
<body>
    <header class="header">
        <a href="/" class="brand">
            <img src="/images/profilepic.jpeg" alt="Tom Ip">
            <span>TOM IP</span>
        </a>
        <nav class="nav">
            <a href="/about">ABOUT</a>
            <a href="/blog">BLOG</a>
            <a href="/newsletter">NEWSLETTER</a>
            <a href="#" class="youtube-badge">1.2M</a>
        </nav>
    </header>

    <main>
        <div class="content">
            {% block content %}{% endblock %}
        </div>
    </main>
</body>
</html> 